- if AdditionalTags.setting?(:active_wiki_tags) && User.current.allowed_to?(:add_wiki_tags, @project)
  p#wiki_tags
    = label_tag :wiki_page_tag_list, l(:field_tag_list)
    = render 'wiki/tags_form', project:, page:
